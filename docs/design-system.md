# Design system — the norm for every screen

> **Status: the single source of truth for look, behaviour and copy** of this
> code base, since 9 September 2026. It replaced the prose of
> `docs/design-system.html` — deleted with #783, still in git history — and the
> normative sections of `docs/ui-conventies.md`, which keeps only the IST
> inventories and work lists. The components themselves are not described here in
> pixels: they are rendered live from the real macros at **`/admin/design-system`**
> (#783). Decisions are dated and carry their issue number so nothing is dropped
> silently.
>
> Enforcement: `backend/tests/test_ui_conventions_gate.py` (mechanical rules),
> `test_layer_gate.py` and `test_template_variables_gate.py` (where logic lives),
> the `design-conformiteit-bewaker` agent (judgement, on request). Measured state:
> `docs/ui-conformiteit.md`.

## 0. How to use this document

Every value here exists as a design token (generated by `scripts/build-css.sh`)
and every component as a Jinja macro in `_macros.html`. When you build a screen
you pick a **screen type** (§3), name the **interaction patterns** it uses (§4),
and use the macros. Never a loose colour, a hand-styled button, or a behaviour
that is not in §4 — if a screen needs one, this document changes first.

The chain is: **tokens → macros → gate → this guide**. A change to one is a
change to all four, in one pull request.

### Decisions log

| Date | Decision | Issue |
|---|---|---|
| Jul 2026 | Postal code is always a `<select>` from the lookup table, never free text | — |
| Jul 2026 | Public activity registration is a narrow modal; "Wie doet er mee?" is one compact inline line | #601 |
| Aug 2026 | Creating never opens a modal; a full-page editor instead | #627 |
| Aug 2026 | Records list: fixed top-to-bottom order (title, "+ Nieuwe", search, filters, cards) — no blue bar | C1 |
| Sep 2026 | "Vereffend" is the direction-neutral word on a paid charge and a paid-out refund | #617 |
| Sep 2026 | "Terug te betalen" is yellow (pending), the type badge "Terugbetaling" is orange; teal is not a semantic tone | #660 |
| 8 Sep 2026 | After a successful save you stay where you are, with a toast; no redirect, no "Opslaan en sluiten" | #726 |
| 8 Sep 2026 | "Bewerken" stands directly left of "Verwijderen"; nothing between them | #722 |
| 8 Sep 2026 | A hidden initial state is hidden in the server markup too | #726 |
| 8 Sep 2026 | Out-of-band belongs to a fragment response, never to a full-page swap | #748 |
| 8 Sep 2026 | The question above its options uses `ui.vraag()`, not `ui.label()` | #749 |
| 8 Sep 2026 | Amount fields are text with `inputmode="decimal"` and accept a comma | #735, #769 |
| 9 Sep 2026 | Prose in Markdown, components live from the app; the HTML mock retires | #783 |

Decisions still open are listed in §12.

## 1. Foundations (tokens)

The atoms. Everything refers to them; no loose hex codes or ad-hoc sizes
anywhere. The one canonical source is the Tailwind config in
`scripts/build-css.sh`; the browser renders what is there, so that is the truth,
not a hex repeated elsewhere.

### 1.1 Colour

The Raak palette, warm and recognisable. Ocean Blue carries the interface; the
warm accents are semantic and used sparingly. (Whether the *admin* shell keeps
this palette in a multi-tenant product is an open decision, §12.)

| Token | Value | Role |
|---|---|---|
| Ocean Blue — `blue-700`, `--primary` | `#0051a4` | primary · headings · buttons · chrome |
| Link Blue — `blue-500`, `--link` | `#2367bd` | text/prose links, always underlined |
| `blue-800` | `#02407c` | **only** the hover state of the primary button — never the resting colour of chrome or headings |
| Golden Yellow | `#ffce00` | accent · emphasis |
| Dark Green | `#005d29` | paid · success |
| Cool Green | `#3aba9b` | positive accent |
| Watermelon Red | `#ee3a37` | error · delete |
| Pumpkin Orange | `#f16532` | attention · outstanding |
| Hot Pink | `#f17fb2` | playful accent |
| Indigo | `#460359` | admin shell · category |

Neutrals and surfaces: Ground `#eef3f9` (page background), Surface `#ffffff`
(cards), Surface-2 `#f7fafd`, Ink `#14171c` (body text), Ink-soft `#52607a`
(secondary text, labels), Line `#d7e0ec` (borders).

Rules:

- Allowed blue tints: `blue-700` (primary), `blue-600` / `blue-50` (outline),
  `blue-50` (KPI tiles). **`blue-800` and `blue-900` are forbidden** outside the
  primary hover. The gate enforces it.
- Links carry the brighter Link Blue, underlined, so they pop next to the deep
  brand blue of headings — never a generic Tailwind link blue. Contrast on white
  ≈ 5.5:1.
- Semantic colour (success, attention, error) is separate from the accents. The
  "warmth" comes from the soft ground and whitespace, not from coloured text on
  coloured surfaces.
- Text meets 4.5:1 (3:1 at ≥ 18 px), verified per token.

### 1.2 Typography

Radio Canada Big for headings (display, `font-brand`), Inter for body and
tables — a deliberate readability deviation from the brand guide, which asks for
Radio Canada Big everywhere. Both vendored under `/static`, never from a CDN.
The header (RaaK + tagline) is entirely `font-brand`; the wordmark is **RaaK**.

| Role | Size / weight |
|---|---|
| Page title | 30 / 700 |
| Section title | 22 / 600 |
| Card title | 17 / 600 |
| Body | 15.5 / 400 |
| Label | 13 / 600 |
| Small | 12.5 / 400 |

Line height 1.15 for headings, ~1.55 for text. Headings, section titles, card
titles and status chips are semibold (600) — airy, not heavy. Extra-bold is
reserved for the page title, KPI figures and the brand word.

### 1.2a Mobile-first type scale (#804)

**80% of visits are on a phone.** At that ratio the phone is not a variant of the
design — the phone *is* the design, and a wide screen is the exception that adapts.
So the bare Tailwind class is the phone size, and a `md:` variant appears only where
a wide screen genuinely wants something else.

| Role | Phone (bare class) | Wide screen (`md:`) |
|---|---|---|
| Reading text on a public page | `text-base` (16) | `md:text-sm` (14) where the wide layout was tuned for 14 |
| Card title, year heading | `text-xl` (20) | `md:text-lg` (18) |
| Caption / metadata that is *not* read in sequence | `text-sm` (14) | — |
| Wordmark | `text-3xl` (30) | — |

Why this exists: before #804 there was **one** scale, chosen on a wide screen, and
it applied unchanged on a phone. `text-sm` is a fine caption on a desktop; on a
device held at arm's length it is the body text, and 14 px is then too small. The
tell was that the only element the owner did *not* call too small was the CMS intro
— the one element with no size class at all, inheriting the 16 px base.

Two rules that follow from the table and are easy to get wrong:

- **Headings move up with the body.** Lift the reading text to 16 and leave a card
  title at 18 and the step between them almost disappears — the page reads flat.
- **`text-sm` is a caption again, not body text.** Use it for metadata that is
  scanned rather than read. If a visitor is meant to read the sentence, it is
  `text-base` on a phone.

A block that only ever renders on one breakpoint needs no variant: the mobile menu
is `md:hidden`, so `text-base` is enough, and the wide navigation is `hidden md:flex`,
so its `text-sm` stays as it is.

**Buttons follow the same rule** (#810). `btn_class` carries the phone size bare and
the desktop size behind `md:`, so a wide screen keeps exactly what it had:

| Size | Phone | Wide screen |
|---|---|---|
| `xs` | `px-3 py-1.5 text-sm min-h-11` | `md:px-2.5 md:py-1 md:text-xs md:min-h-0` |
| `sm` | `px-3.5 py-2 text-base min-h-11` | `md:px-3 md:py-1.5 md:text-sm md:min-h-0` |
| `md` | `px-4 py-2.5 text-base min-h-11` | `md:py-2 md:min-h-0` |

Why: #804 lifted the reading text next to a button from 14 to 16 px while the buttons
stayed at `text-xs` (12). The gap went from 2 px to 4 and the button read as
something that did not belong. Rebuilding v1.14 does **not** fix that — the same
buttons were `text-xs` there too, with even less padding. On a phone this makes rows
of buttons taller; that is intended, not a side effect.

**Icon before text that can wrap** (#810): `items-start`, never `items-center`.
Centring aligns the icon to the middle of the *whole* block, so a date spanning three
lines leaves the icon floating in the middle of the sentence.

**The top margin is calculated, not guessed** (#813): `(line-height − icon height) / 2`.
The icon is 16 px, the line is not, and `items-start` aligns their *tops*.

| Text class | Line height | Margin |
|---|---|---|
| `text-sm` | 20 px | `mt-0.5` |
| `text-base` | 24 px | `mt-1` |
| `text-base md:text-sm` | 24 / 20 px | `mt-1 md:mt-0.5` |

Write that last form as-is: a bare `mt-1` is 2 px too much on a wide screen, and
that is what a gate checks. Without the rule the next text size gets a number that
merely looks about right — which is exactly how #813 happened, one issue after #810.
With the margin in place the single-line case looks identical, so there is no reason
to choose per case.

**Touch targets.** A control a finger has to hit is at least 44 px in both
directions (`min-w-11 min-h-11`); the icon inside it may be smaller. The hamburger
is the reference case — it was the text character `☰` without a size class, which
inherits weight and height from the device font and therefore cannot be scaled
reliably. Use `icon("menu")`, like every other symbol control.

### 1.3 Spacing, radii, elevation

One 4 px scale: 4 (icon margin) · 8 (button padding) · 12 (field gap) · 16 (card
padding) · 24 (between cards) · 32 (section) · 48 (large section) · 64 (page
margin). Radii: 8 px fields and buttons, 12 px panels, 18 px cards, pill for
chips. `shadow-lg` for modals only.

### 1.4 Icons

Lucide, one line style (1.75 px, rounded ends), following the text colour,
self-hosted as an SVG sprite via `ui.icon()`. Loose glyphs (⬇, 📄, ▲▼) are not
a choice but a remnant from before the helper; the gate rejects them.

**One meaning per glyph** (#698):

| Glyph | Means | And only that |
|---|---|---|
| `trash-2` | delete | always red (`btn_danger`) |
| `pencil` | edit | not the gear — that means *settings* |
| `×` | close | a toast, a modal, a panel — never delete |

`ui.icon()` fails silently on an unknown name (empty SVG), so the gate asserts
the rendered `<path>`, not the macro call.

## 2. Components

Each component is built once, has a macro name, and is identical everywhere.
The live rendering with every variant is the route `/admin/design-system`
(#783). Below: the macro, and the rules a screen must respect when using it.
Import with `{% import "_macros.html" as ui %}`.

| Group | Macros |
|---|---|
| Structure | `page_header` · `section_header` · `section_bar` · `card` · `nested_panel` · `tabs` · `detail_disclosure` |
| Lists | `search` · `filter_bar` · `grouped_filter` · `chips` · `pager` · `row_actions` · `reorder` · `empty_state` · `loading` |
| Forms | `field_input` · `field_select` · `field_textarea` · `label` · `vraag` · `input_control` · `select_control` · `textarea_control` · `person_fields` · `upload_field` · `export_links` · `copy_button` |
| Feedback | `toast` · `toast_oob` · `toast_host` · `success_banner` · `error_banner` · `modal` · `confirm_host` · `badge` |
| Buttons | `btn_primary` · `btn_secondary` · `btn_outline` · `btn_danger` · `button` · `btn_class` · `edit_toggle` · `icon` |

### 2.1 Buttons

- One filled primary action per screen; the rest calmer. Never outlined buttons
  everywhere, never "Verwijderen" next to the primary action.
- Loading and disabled states come from the macro. A submit locks after click
  and reads **"Bezig…"**.
- Icon-only buttons need an `aria_label`; `button()` then adds a `title` with
  the same text, so mouse users do not have to guess either (#698).

### 2.2 Form fields — two families, not interchangeable (#659, #663)

- **`ui.field_input` / `field_select` / `field_textarea`** — a field in an
  ordinary vertical form. They bring label, `mb-4` block, hint and error line,
  and derive the `id` from the `name`. Usable only when that `name` is unique on
  the page.
- **`ui.input_control` / `select_control` / `textarea_control`** — a loose
  control in a compact or repeated context: you set label, id and width
  yourself. Needed as soon as the same `name` appears more than once (every card
  in a loop has a field `name`), or when the width drives the column in a
  `flex flex-wrap` row.
- Pass your own text size (`text-xs` for a code field) and the kit base drops
  its `text-sm`; otherwise two equal utilities collide and the generated CSS
  order decides, not the attribute.
- A control that sets its own border (`border-gray-300`) is by definition
  hand-written; gate rule 30 catches it.
- All fields the same height, one focus ring, error inline below the field,
  label linked (`for`/`id`), error via `aria-describedby`.
- **Postal code is always a `<select>`** from `PostalCode`, never free text.
  Required is a red `*`. Address grid: 4 columns; street (span 2) + house
  number + box on row one, postal code full width on row two.

**Label or question (#749).** `ui.label()` is a management form: grey
`text-sm font-medium` above a boxed field — the field is the content. `ui.vraag()`
is a public form: `text-base font-semibold text-ink` — the question is the
content, the options are the answer. With `label()` the question rendered
lighter than its own options; weight does not compensate for contrast. The
rhythm belongs to it: section title (`text-lg font-bold`) → question → option
(`text-sm`), with `space-y-6` between questions and `space-y-1` between options.

**Amounts (#735, #769).** An input follows the screen's notation — `€ 35,00`
with a comma — because no amount field is `type="number"`: they are text with
`inputmode="decimal"`, and `_ingetypt_bedrag()` / `_decimal()` accept comma and
point. The gate checks two things separately: no hand-formatted amount
(`"%.2f"|format`), and no `inputmode="decimal"` field with `type="number"`,
where a comma is an invalid value that empties the field silently. Watch where a
value comes from: on the payments screen the same field is filled at render and
by the status choice; convert one and the field shows two notations.

### 2.3 Table, search, filters, row actions, paging

- **Default list = table** in a `card` with `overflow-x-auto`; cards only for
  hierarchical or visual content (§9). Row height ~50 px, subtle hover.
- **Every list that can grow gets `ui.search()`** (rule of thumb: > 20 items
  possible): `type="search"`, placeholder **"Zoek op <velden>…"**, server-side,
  debounced (300 ms), no separate search button; parameter `q` by default.
- **Paging server-side, 50 per page, `ui.pager()`**: "x–y van n" with
  vorige/volgende, no page numbers; hides itself when everything fits. Without a
  total, pass `has_prev`/`has_next` and it shows "Pagina n" — for lists that
  deliberately avoid a `COUNT` (the e-mail log).
- **`ui.filter_bar(hx_get, hx_target)`** wraps search and filters in one form so
  htmx sends every value together; use the fields inside with `standalone=False`
  or the search fires its own request too.
- **Hierarchical axis = one grouped `<select>`** (`ui.grouped_filter`, #549), an
  `<optgroup>` per category ("Alle <cat>" + subtypes), one value encoding both
  levels (`membership`, `membership.reminder`). Separate fields only for
  independent axes (status next to context on Betalingen).
- **Filters and search apply live** (htmx on change / debounced input); no
  "Toon" button. Filters sit below "+ Nieuwe" and search, never above.
- **Sorting**: fixed, documented order until column sorting exists (newest first
  for logs, alphabetical or `sort_order` for management). Every ordering carries
  a unique tiebreaker (#761).
- **Manual reorder = `ui.reorder()`**: arrows follow the layout (vertical ▲▼,
  grid ◀▶), identical style; visible click target, never a glyph that reads as a
  spinner (#702).

### 2.4 Row actions (#722, #698)

- **List rows**: 2–3 actions visible, the rest under `⋯` (`row_actions`,
  `max_visible` default 2). A guideline, not a hard cap; with few rows on a wide
  screen everything may be inline.
- **Detail screens and toolbars: no cap.** A `⋯` menu hiding one action is worse
  than showing everything.
- **"Verwijderen" last and red**, in the `⋯` menu once it exists. Never an
  emoji, never hidden: if not allowed (money moved) show it disabled with a
  tooltip reason.
- **"Bewerken" directly left of "Verwijderen"**, nothing between them. The
  `edit_toggle` shows "Bewerken" in read mode and "Annuleren" in edit mode, in
  the same place. Anything acting on the *content* (view, print, export, import)
  stands left of that pair. A button between the two breaks the pair and puts
  the most dangerous button next to an arbitrary neighbour.

### 2.5 Status chips (badges)

`ui.badge(label, tone)`, pill `px-2 py-0.5 rounded-full text-xs font-semibold`.
Every status gets everywhere exactly the same tone, shape and Dutch name; never
a raw code. Status is conveyed by the badge only — no coloured card stripes or
tinted backgrounds.

| Meaning | Tone | Examples |
|---|---|---|
| ok · active · paid · handled | green | Open, Betaald, ✓ Behandeld, **Vereffend** |
| pending · waiting | **yellow** (amber is dropped) | Openstaand, **Terug te betalen**, Gewijzigd |
| error · deleted | red | Mislukt, Verwijderd, Geannuleerd |
| full · attention (not an error) | orange | Volzet, **Terugbetaling**, Deels betaald |
| draft · inactive · off | grey | Concept, Overgeslagen |
| info · role | blue (`blue-100` / `blue-700`) | ADMIN, FINANCE |

- **"Vereffend"** (#617) is used on the paid charge *and* the paid-out refund:
  direction-neutral, where "Betaald" reads as money coming in.
- **"Terug te betalen" is yellow** (#660): the same kind of state as Openstaand
  (money still has to move); the direction is read from the separate orange type
  badge "Terugbetaling". "Deels betaald" stays orange: not a waiting movement but
  an incomplete one.
- **Teal is not a semantic tone.** The macro knows it; a status or type never
  uses it. A gate rule keeps it out.
- Labels come from one place per code list, never from a dict in a screen
  (#779).

### 2.6 Upload field (#623, #654, #655)

One shape wherever a file belongs to a record. Fixed order: **label → file
picker → current attachment (link + delete) → hint**. Macro
`ui.upload_field(name, text, current_url, delete_attrs, hint)`.

- **No submit button of its own.** The block belongs to the form around it, so
  one "Opslaan" saves text and file together — at every level (activity,
  component, product). One record in edit mode shows **exactly one** "Opslaan";
  two means two forms to two endpoints, and that becomes one form. Reference:
  `activiteit_bijwerken` in `activities/admin_ui.py`.
- The enclosing form needs `enctype` and `hx-encoding` `multipart/form-data`.
- **Delete stays a separate action**: it is not a save.
- **The current attachment is never on screen twice.** In edit mode it lives in
  the upload block; a read link outside it exists **only in read mode**
  (`x-show="!edit"`), guarded so that without an attachment there is no link.
  A read view living next to an edit form is always behind `x-show="!edit"`
  (#648, #653).
- With a URL field next to it, the hint says an uploaded file takes precedence
  (#223).

### 2.7 Notifications, modal, toast, confirm

- `ui.error_banner()` — red, `bg-red-50 text-red-700 rounded-lg p-3`, at the
  top of the page or form. Stays until the cause is fixed.
- `ui.success_banner()` — for a lasting result (a thank-you screen); otherwise a
  toast.
- `ui.toast()` — short confirmation that fades on its own (4 s); `kind="error"`
  does not fade. Rendered as a fragment, so a response can send it out-of-band
  (§8.2 says when). `ui.toast_host()` is already in both shells.
- `ui.modal()` — X + Esc + backdrop, `role="dialog"`. Modals are for read-only
  detail, confirmations, and the one sanctioned capture (§3.3).
- `ui.confirm()` — confirmation before a destructive action, on `ui.modal()`;
  never `confirm()`, never `hx-confirm`.
- **Copy to clipboard confirms inline** (`ui.copy_button`, #689): the button
  shows a check mark where you clicked. A toast is for something that happened
  elsewhere; this happened under your cursor. On failure the check mark stays
  away and the text is selected: the button does not lie. Never inside an `<a>`
  — use the stretched-link pattern.

### 2.8 Loading and empty

"Laden…" via `ui.loading()` (fixed height, no layout jump). After a filter or
search: **"Geen resultaten gevonden."**; never any data: **"Nog geen
<items>."** — one sentence, not italic, no emoji, no call-to-action push.

### 2.9 Pivot table (#834)

The second shape of a report: rows × columns × measures, with a subtotal per row
group and a grand total. `ui.pivot_table(pivot)` renders it from a plain dict, so
the macro stays kit code and knows nothing about reporting.

- **Every number comes from the server.** The macro adds nothing up — not even a
  row total that looks free. An average over three columns is not the average of
  three averages, and a distinct count over two months is not the sum of two
  distinct counts. The engine asks SQL four times (the grid, the row totals, the
  subtotals, the grand total); the macro only places the answers.
- **An empty cell shows an em dash**, never a zero. "No such combination exists"
  and "the total is nothing" are different statements, and a reader cannot tell
  them apart once both read `0`.
- **Horizontal scrolling stays inside the card** (§2.3), and the first column
  stays put (`sticky left-0`) so you still know which row you are reading at
  column twelve.
- **Subtotals are distinguished with tokens** — `bg-gray-50` and a heavier weight,
  no colour of their own. A subtotal line carries the label of its group only; the
  finer columns stay empty rather than repeating.
- **A crosstab is capped at 30 members on the column dimension**, counted under
  the active filters, with a message that names the dimension. A 400-column
  crosstab is not a report, and "too many columns" without saying which dimension
  leaves the user guessing which of his choices to undo.
- With a single measure the second header row is left out: repeating "Aantal"
  under every year doubles the header height and says nothing.

### 2.10 Charts (#835)

Three kinds, as server-rendered SVG: `ui.chart_bar`, `ui.chart_line`,
`ui.chart_stacked`. No library, no CDN, no data leaving the server.

- **A chart is the crosstab, drawn.** It reads the pivot result and never a second
  query, so it cannot disagree with the table above it. That is also why it is
  cheap: the numbers were already there.
- **The table stays.** A chart is rendered *with* its table, never instead of it —
  the table is the text alternative, and a picture must never be the only way to
  the number. Every chart carries a `<title>` and a `<desc>`, and every bar, point
  and segment its own `<title>` with the value.
- **Zero is always in the domain**, and the axis ends on a round number. A bar
  chart whose baseline is not zero exaggerates every difference on it — the most
  common way a chart lies without anybody writing anything false.
- **Colours come from the tokens** (`var(--brand-…)`), in a fixed order: brand blue
  first because a single-series chart must look like the rest of the application,
  then hues that stay apart; `danger` and `warning` last, because they mean
  something here and a red bar for no reason reads as a problem.
- **Responsive through `viewBox`**, never a fixed width. The scale is computed on
  the server; the macro only places marks. Deciding where zero sits is a rule, and
  a rule in a template is a rule in two places (§8.3).
- Subtotal rows are left out of a chart: drawn beside their own parts they would
  count the same money twice.
- **Not built, on purpose**: pie and donut (a share drawn as a quantity), scatter,
  and a second y-axis (two unrelated scales made to look comparable). Three kinds
  are what a board member needs.

## 3. Screen types

The admin screens are compositions of seven real patterns. Build those well and
every screen follows.

| # | Pattern | Made of | Admin | Public |
|---|---|---|---|---|
| 1 | App shell | sidebar (logo, nav, log out at the bottom) + top bar | every admin page | top nav + footer, no sidebar |
| 2 | Page header | title · optional one-line description · primary action on the right | Activiteiten, Leden… | hero with larger title + main action |
| 3 | List | search + filter bar · status chips · row actions · paging | Betalingen, Leden | activities as cards, no table |
| 4 | Form | sections · fields · save/cancel · inline errors | edit activity or form | Word lid, register (airier) |
| 5 | Detail | header + cards or side panel · related lists | family detail, dashboard | activity detail page |
| 6 | Dialog | modal · side panel · toast · confirmation | everywhere | confirmation after registration |
| 7 | Report | objects pane · selection · filters · result table | Rapporten | — |

Candidates to add when a screen needs them: **wizard** (leden-import) and
**dashboard** (KPI row); today they are sanctioned variants of 4 and 5.

### 3.1 Admin and public — family, not twins

Both share tokens, fonts, icons, buttons, form language and notifications; the
expression differs. Admin: calm, efficient, dense; neutral ground, strong tables
and forms; compact cards, little decoration; fast operation. Public: more
expressive and warmer, larger typography, photos and posters, more Ocean Blue,
activity cards, clear calls to action. The volunteer who uses both sides learns
one interaction language.

### 3.2 Records list — the fixed layout

Every admin screen with records has the same order in the content column, top
to bottom: **page title → one-line description (optional) → KPI row (optional)
→ one "+ Nieuwe <item>" button, primary, standing on its own → search → filters
→ record cards or table.** No blue bar around the create button, no collapsible
form under it; the create form opens as a full-page editor.

Page title is `<h1>` in `text-blue-700` with an optional grey subtitle. The
create button reads **"+ Nieuwe <item>"**, `btn-primary btn-sm`, no exceptions;
read-only screens have none; a create in a sub-context ("+ Persoon" per family)
stands beside it and never replaces it.

### 3.3 Modal or page — the rule (#627, #601)

> One short, **self-contained** action in the context of a list → modal.
> A form you need to take in at a glance, or an object you keep working on
> afterwards → full page.

That covers every case: admin create → page (you carry on in the editor), Word
lid → page (long form), activity registration → narrow modal (`max-w-md`,
contact + quantities and done; the card stays visible underneath). Creating
never opens a modal. Long details go in a side panel, not a modal.

### 3.4 Record management (decided)

From a list, "Bewerken" opens the editor. Complex records (activity, form,
member) use the full-page editor; one-line records (media, users) may be edited
inline in the card as a sanctioned variant. Where a list shows concrete records
(registrations, payments, products) the in-line pattern is `detail_disclosure
(load_url, target_id)`: the trigger loads the **shared** detail fragment once
via `hx-get` into a target; the screen never rebuilds the editor itself. In a
table: trigger in the action cell, target in a detail `<tr>` inside the same
`<tbody x-data="{ open: false }">`. Inside the editor: `[Opslaan] [Annuleren]`
at the bottom, "Verwijderen" last and red; the **server recomputes derived
values** (totals), never the client.

### 3.5 Report — the query panel (#833, CR-06 §5)

The only screen where the user composes the query instead of reading a prepared
one, so it is its own type rather than a variant of the list. Three regions, in
this order:

1. **Objects** — the universe grouped in classes, each object with a type marker
   (Σ measure · ▦ dimension · · detail) and its description as a tooltip. A click
   adds a column; a dimension also offers "filter". Searching the list happens in
   the browser: the whole list is already there, and a request per keystroke would
   swap the field out from under the user's fingers.
2. **Selection** — the chosen objects in order, each with `ui.reorder()` and a
   remove button. Order is column order. No drag-and-drop: it costs a library or a
   lot of Alpine, and the value is in the universe, not in the gesture.
3. **Filters** — one control per chosen or extra dimension. A closed list renders
   as `ui.select_control` and compares exactly; anything wider renders as a search
   field and searches. Live (P11): no "Toon" button.

The **result** is a C1 table: sortable headers, server-side paging, a totals row
for the measures, the one money formatter, and a default sort that ends in a
unique key (#761). A dimension that points at a record links through to it (P8) —
a report is a way in, not a dead end.

**The whole panel state lives in the query string**, and every change is one htmx
request that re-renders the *whole* panel. Not only the result: the hidden inputs
that carry the state and the screen that shows it must always say the same thing,
and refreshing half of them is how they start to disagree. A shared link therefore
opens the same report, and a refresh changes nothing.

Saving follows P1 — you stay, a toast appears, and a failed save shows the banner
**and no toast**.

## 4. Interaction patterns

Behaviour is where "modern" lives as much as in colour. A screen names the
patterns it uses; a new behaviour is a new pattern here first. Each pattern has
the same six fields: **when · what happens · where attention goes · the way
back · forbidden · test.** Fields marked *open* are decisions still to take
(§12) — they are written down as open rather than left implicit.

### P1 · Edit and stay (#726, #717)

- **When**: saving an existing record, in a full-page editor or an inline panel.
- **What happens**: the record is saved; the screen shows what is now really
  stored; a toast "Opgeslagen ✓" appears. In an inline panel the panel closes
  and the list is still there. In a full-page editor the screen stays.
- **Where attention goes**: *open* — the toast is top-right; scroll and focus
  after save are not yet specified.
- **The way back**: the return link at the top ("‹ Alle activiteiten"). You go
  back when *you* are done, not when the system decides you are.
- **Forbidden**: a redirect to the list (throws your place away; a confirmation
  that survives navigation needs a flash mechanism we do not have); a second
  primary button "Opslaan en sluiten" (two good answers every time); a toast on a
  failed save.
- **Test**: success shows the confirmation; a failure shows the error banner
  **and no toast**; for a full-page response the toast is asserted **e2e**, not
  server-side (#748).

### P2 · Create and continue (#627)

- **When**: "+ Nieuwe <item>" on a records list.
- **What happens**: a full-page editor opens; after the first save you are in
  that record's editor (P1 applies from then on).
- **Where attention goes**: the first field of the new form.
- **The way back**: the return link to the list.
- **Forbidden**: a modal or a collapsible form above the list; a dialog whose
  few fields rarely make the object complete.
- **Test**: the list shows no create modal; the create route renders the
  full-page editor.

### P3 · The way back

- **When**: any screen reached from another (registration from an activity,
  detail from a list).
- **What happens**: a return link at the top names where you came from.
- **Where attention goes**: *open* — which position in the origin list you land
  on, and whether filters are preserved, is not yet specified (the guide
  promises "filters preserved after returning"; not verified, §5).
- **The way back**: *open* — from a registration opened via an activity, the
  link should return to that activity, not to "all registrations". Decision
  needed on how the origin is carried (referrer, query parameter, or the
  activity as the only entry).
- **Forbidden**: relying on the browser back button as the only way.
- **Test**: the return link exists and points at the origin.

### P4 · Action with a result elsewhere

- **When**: a control whose effect appears somewhere else on the page (the JSON
  import in the form builder: button at the top, import card at the bottom).
- **What happens**: *open*. Either the result comes to the control (a panel
  opens in place), or the control lives next to its result. Today the builder
  carries a workaround in a template comment; that is the symptom this pattern
  exists to remove.
- **Where attention goes**: to the result, without the user scrolling to find it.
- **The way back**: closing the panel returns the page to its previous state.
- **Forbidden**: a control and its result out of each other's sight.
- **Test**: e2e — after the action the result is in the viewport.

### P5 · Confirm a destructive action (#722)

- **When**: deleting a record, or any action that cannot be undone.
- **What happens**: `ui.confirm()` with the fixed copy **«<Type> "<naam>"
  definitief verwijderen?»** plus one consequence line where relevant («Alle
  inzendingen worden mee verwijderd.» / «Het feit blijft in de audit-historie
  bewaard.»). Buttons: red "Definitief verwijderen", secondary "Annuleren".
  `confirm_attrs(type, name)` is for real deletes only; a confirmation that is
  not a delete ("Bevestig betaald?", "Importeren?") or that spells out a
  consequence keeps its own `data-confirm` text.
- **Where attention goes**: the dialog; focus on "Annuleren".
- **The way back**: "Annuleren" or Esc returns unchanged.
- **Forbidden**: `confirm()`, `hx-confirm`, "Verwijderen" next to the primary
  action, a delete without confirmation.
- **Test**: cancelling leaves the record; confirming removes it and the list
  updates.

### P6 · Import in steps

- **When**: bulk input that can go wrong halfway (leden-import).
- **What happens**: dry run → report → explicit commit whose label names the
  consequence ("Definitief importeren"). A wizard only when there are sections or
  branching; short captures are one page with sections.
- **Where attention goes**: the report of the dry run.
- **The way back**: leaving before the commit changes nothing.
- **Forbidden**: a commit without a dry run; a wizard for a short form.
- **Test**: the dry run writes nothing; the commit writes what the report said.

### P7 · Error with reason (#723, #570)

- **When**: any failed load or save.
- **What happens**: a red banner at the top of the page or form with the
  server's reason (`parseApiError`), errors beside the field where the field is
  known. The banner stays until the cause is fixed.
- **Where attention goes**: the banner, and the first invalid field.
- **The way back**: not applicable; the user corrects and retries.
- **Forbidden**: `alert()`; `.catch(()=>{})`; a generic sentence where the
  server sent a reason; a message stuffed into a placeholder; a bare 422; a
  success toast alongside the error.
- **Test**: the failure path asserts the reason text, not just a status ≥ 400
  (#680).

### P8 · List, detail, edit (#510)

- **When**: a list of concrete records.
- **What happens**: the row's primary action opens the shared detail fragment
  in place (`detail_disclosure`); "Bewerken" toggles the fragment into edit mode
  (`edit_toggle`); save follows P1 and the panel closes.
- **Where attention goes**: the opened panel, directly under its row.
- **The way back**: closing the panel; the list has not moved.
- **Forbidden**: a second copy of the editor built by the list screen; an edited
  row that jumps to the bottom (ordering without a tiebreaker, #761).
- **Test**: the fragment is rendered from one template; after save the row
  keeps its position.

### P9 · One-shot public capture

- **When**: lid worden, a public form, an idea — something a visitor submits
  once.
- **What happens**: a lasting thank-you screen replaces the form: «✅ <wat>
  ontvangen!» plus what follows («Je ontvangt een bevestiging per e-mail…»).
  Small actions get a toast instead. Submit reads "Bezig…" while pending.
- **Where attention goes**: the thank-you screen.
- **The way back**: to the site (`/`), or to the source for a cancelled payment
  — never to a route that does not exist.
- **Forbidden**: a five-second banner that disappears; a redirect that loses the
  confirmation.
- **Test**: after submit the form is gone and the thank-you text is present.

### P10 · Copy to clipboard (#689)

See §2.7: confirms inline, never with a toast.

### P11 · Filter and search live

- **When**: any growable list.
- **What happens**: typing (debounced) or changing a filter reloads the list
  fragment; all values travel together via `filter_bar`.
- **Where attention goes**: stays in the field; the list updates below.
- **The way back**: clearing the field or choosing "Alle …".
- **Forbidden**: a separate "Toon" button; two requests per keystroke.
- **Test**: one request per change; the filter state matches the list.

### P12 · Navigate without losing the shell (#634, #718, #737)

- **When**: any link between admin screens.
- **What happens**: `hx-boost` swaps the content, not the shell — no white
  flash, the sidebar stays, the tab title follows; a 2 px progress bar in
  `--brand-ocean` at the top while a request is in flight, and the requesting
  element dims to 60 % and stops accepting clicks. Downloads, logout and links to
  the other shell opt out with `hx-boost="false"`.
- **Where attention goes**: the top of the new content.
- **The way back**: browser history works, because boosted navigation pushes a
  URL.
- **Forbidden**: navigation that disappears after a save (#718); a mobile menu
  that opens after every boosted navigation (#737).
- **Test**: e2e across **two** actions — every finding in this class sat
  between two actions, not inside one (#759).

## 5. Behaviour rules and their status

The rules below apply everywhere. The status column says whether the code base
is known to do it, measured on 9 September 2026 — a rule that reads as "done"
while it is an intention is worse than no rule.

| Rule | Status |
|---|---|
| Save without unexpected page jumps (htmx) | in place; scroll/focus after save unspecified (P1) |
| Visible loading state; the button locks after click | in place (macro) |
| Navigation swaps the content, not the shell; progress bar | in place (`hx-boost` in 25 templates, `htmx_ux`) |
| Clear toast after saving | in place for 6 of 92 mutating routes; the rest is #760 |
| Warning on unsaved changes | **not built** — no `beforeunload` anywhere; keep as intention or drop (§12) |
| Filters preserved after returning or polling | not verified |
| Filters and search apply live, no apply button | in place |
| Empty-state copy: one sentence per case | in place (macro) |
| Errors beside the relevant field | in place (macro) |
| Destructive actions never next to the primary action | gate rule (#722) |
| Visible keyboard focus and correct tab order | not verified |
| Smart default values (media defaults to Activiteitenfoto, #708) | per screen |
| Never `alert()` or `confirm()` | gate rule |

## 6. Copy and terminology

Fixed words, everywhere: **Opslaan** (create and edit; never "Bewaar",
"Aanmaken") · **Annuleren** · **Bewerken** · **Verwijderen** · **"+ Nieuwe
<item>"** · **"Zoek op <velden>…"** · **Vorige / Volgende** · **"Laden…"** ·
**"Bezig…"** · **"(Nog) geen <items>."** · **"Geen resultaten gevonden."**
"Toevoegen" only for adding a sub-item or line to a collection inside a form.

- Button order at the bottom of a form: **[Opslaan] [Annuleren]**, left-aligned.
  The `edit_toggle` at the top does not replace them: the bottom pair is where
  attention is when you save; on a long form the top is out of view.
- The core action of an activity is **"Inschrijven"** — on the card, in the
  modal title and on the submit. Not "Schrijf je in".
- **No vendor names in labels**: "Status verversen", not "Ververs bij Mollie".
- Titles and badges always Dutch; never a raw status code. Sentence case;
  je/jij; one error fallback: «Er is iets misgelopen. Probeer opnieuw.»
- Money: one formatter, "gratis" and member-price variants included; never a
  raw `toFixed`/`"%.2f"` in a template.

## 7. The public side

Same kit, warmer expression. Specifics beyond §3.1:

- **CTA hierarchy**: the core action of a page is always `btn-primary`
  (`btn-sm` in a list); secondary `btn-secondary`; tertiary (Info ↗, "Wie doet
  er mee?") may stay a text link. "Wie doet er mee?" is one compact inline line
  in `text-xs text-gray-600`: *N ingeschreven — naam · naam* — never a vertical
  list, never a wide inline block (#601).
- **Registration is a narrow modal** (`max-w-md`, × / Esc / backdrop) — never an
  inline tinted block that widens the card.
- **Payment flow**: default method online (Mollie); on success with a
  `checkout_url` a **hard redirect** via the `HX-Redirect` header, never a
  client-side navigation. Cancelled → back to the source, or `/`. Widget copy:
  online = redirect explanation; transfer = «rekeninggegevens per e-mail».
- **One payment-method vocabulary** across flows: canonical `online` /
  `transfer`, labels "Online betalen" / "Overschrijving".
- **Navigation** marks the active link (underline or fixed background) with a
  text cue for screen readers — colour alone is not a marking (#608).
- **Tenant branding is configuration, never a hard-coded default** (#519):
  tagline, social links and the privacy link come from tenant settings; empty
  means not shown. Fixed inline content (`site-footer`, `home-intro`) is
  addressed by a fixed slug and edited per tenant in the CMS (#457).
- **CMS editor is Trix**, vendored under `/static/vendor` (no CDN, no data
  leaving the EU); file attachments disabled; stored HTML is sanitised
  server-side at every render point (#476, #520).

## 8. Technical rules (htmx, Alpine, Jinja)

### 8.1 A hidden initial state is hidden in the server markup (#726)

An element with an `id` that Alpine hides must also be hidden in the server
markup: `style="display: none"` next to `x-show` and `x-cloak`. `style` is in
htmx's `attributesToSettle`: after a swap, an element with an `id` gets the
attributes of the **server response** back after the settle delay, wiping the
`display: none` Alpine set 50 ms earlier. `x-cloak` does not help — Alpine did
run; the damage comes after. Only with an `id`, and only when hidden is the
correct initial state.

### 8.2 Out-of-band belongs to a fragment response (#748)

An out-of-band element does not survive a full-page swap. htmx places the oob
toast in `#toasts` and then replaces the whole body — host included — with the
fresh, empty one from the same response. So: **fragment response** (lands in a
small target) → `ui.toast_oob()`; **full page** (`hx-target="body"`) → the
confirmation rendered **inside** its host via `ui.toast_host(bevestiging=True)`
from the shell. Nine forms in this app do a body swap; for every new
out-of-band feature this is the first question. A server test cannot see this
failure: it checks that the response *contains* the toast, and it does. Assert
visibility, e2e.

### 8.3 Where logic lives — view-models and strict templates (#643, #635)

Jinja checks nothing up front. A template that reads `total` while the route
passes `totaal` renders an empty string, and no test fails until someone looks.
Five rules replace that safety net:

1. **An undefined variable is an error, not a blank.** Dev, test and HDEV run
   `StrictUndefined`; UAT and PROD render empty *and* log a WARNING on
   `app.ui.undefined`. The environment is built in `app/ui/__init__.py`, where
   `autoescape=True` is set explicitly.
2. **Every screen route passes a view-model**: a `@dataclass(frozen=True,
   kw_only=True)` subclassing `app.ui.viewmodel.ViewModel`, in
   `<domain>/viewmodels.py`; the route ends with
   `templates.TemplateResponse(request, "x.html", vm.as_context())`. Fragments
   get one too.
3. **Optional is explicit**: `|default(...)` or `is defined`, with a one-line
   comment saying why. Never switch strictness off for one template.
4. **A UI route builds a view-model and picks a template.** Money, state and
   authorisation live in `<domain>/service.py`, called by the JSON router and
   the UI route alike, with a service test. A UI module holds `db` only to pass
   it on.
5. **Two gates keep it that way**: `test_layer_gate.py` (imports, ORM,
   view-models) and `test_template_variables_gate.py` (a template asks for
   nothing its model does not promise). Both carry a shrinking allowlist.

## 9. Sanctioned exceptions

- **leden**: master-detail (family list + detail) — keeps standard search,
  paging and feedback.
- **media**: thumbnail grid with ◀▶ (grid direction); upload feedback via
  banner or toast.
- **leden-import**: two-step wizard (dry run → commit), explicit commit label.
- **paginas**: rich-text editor (Trix).
- **activiteiten**: nested cards (activity → dates and components → products);
  card shape stays, actions, glyphs and feedback follow §2.
- **read-only screens** (dashboard, analyse, info, ledenwijzigingen): no create.
- **public**: FamilyRegistrationForm one page (no wizard); ChatWidget compact
  floating panel with kit colours; PhotoGallery lightbox dark overlay
  (`bg-black/80`) with Esc; shared members/admin login screen.

## 10. Accessibility (WCAG)

A hard requirement, not an extra.

| Category | Rule |
|---|---|
| Contrast | ≥ 4.5:1 text · 3:1 large text — verified per token |
| Focus | visible focus ring on everything operable |
| Semantics | real `<button>` / `<nav>` / `<main>`; no `<div>` as a button |
| Keyboard | all functionality reachable without a mouse |
| Icons | `aria-label` on icon-only buttons; decorative = `aria-hidden` |
| Forms | label linked to input; error via `aria-describedby` |
| Motion | `prefers-reduced-motion` respected |

An axe-core check in CI is a candidate for the gate; not built yet.

## 11. Governance

Consistency is enforced technically, not hoped for.

1. **Tokens** — one source, `scripts/build-css.sh`. Templates refer to them.
2. **Macros** — every component in `_macros.html`. Change a button and it
   changes everywhere.
3. **Gate** — `test_ui_conventions_gate.py` forbids raw colour utilities, loose
   button classes, `alert()`/`confirm()`/`hx-confirm`, `amber-*`, teal as a
   status tone, symbol-only buttons without `aria-label`, hand-written controls.
   A **new rule-shaped** deviation becomes a gate rule, not a manual check each
   release. Judgement stays with the `design-conformiteit-bewaker` agent.
4. **The live page** — `/admin/design-system` renders every macro from the real
   `_macros.html` with the real `app.css`, so the visual reference cannot drift
   from the code. A completeness gate fails CI when a new macro has no demo.
   What it *cannot* check is whether a demo is representative: it sees presence,
   not fidelity. A macro is called there with the arguments a real screen gives
   it, and each section links to that real screen — read those links as part of
   the page, not as decoration.
5. **This guide** — a living document. A rule change, its macro change and its
   gate change land in **one** pull request. The Dutch reasoning behind a rule
   travels with it: the *why* is what makes a rule survive the next reviewer.
5. **The live route** `/admin/design-system` (#783) renders every macro with
   its variants from the real kit and CSS, so the visual reference cannot drift
   from the code. Until it exists, the reference is the macro file.

Two claims of the former HTML guide are **not** in place and are listed as
intentions: Playwright golden screenshots of the reference screens (the e2e
"golden flows" are flow tests, not screenshot comparisons), and the axe-core
check.

End state: it becomes hard to build an ugly or inconsistent screen — the
shortest path is the right path.

## 12. Open decisions

Raised on 9 September 2026; each needs Koen's decision before it becomes a
rule. Until then the pattern says *open*.

1. **Product brand for the admin, tenant brand for the public side.** The admin
   shell is Raak (indigo, wordmark, palette). In a multi-tenant product a second
   client should not see Raak in the back office. Decide on four mockups
   (payments list, activity detail, registration panel, form builder); then the
   tokens change and every screen follows through the macros.
2. **Attention after an action** (P1, P4): where scroll and focus go after a
   save, and how a control relates to a result elsewhere on the page (the JSON
   import). Today unspecified; the builder carries a workaround.
3. **The way back from a registration opened via an activity** (P3): to the
   activity, not to all registrations. How the origin is carried.
4. **Warning on unsaved changes**: promised, never built. Keep and build, or
   drop the promise.
5. **Screen types 7 and 8**: wizard and dashboard as first-class types, or keep
   them as sanctioned variants.

## 13. Migration — current → target

| Current | Target | Status |
|---|---|---|
| Title colour varies | always `--primary`, Radio Canada Big | gate |
| Body = system sans | Inter vendored under `/static` | done |
| Amber and yellow mixed | pending = yellow, amber dropped | gate |
| Search on 3 of 15 lists | every growable list: `ui.search()` + `ui.pager()` | per screen (#758) |
| Three reorder glyphs | one `ui.reorder()` | done (#702) |
| Row actions: boxed / text / emoji | one `ui.row_actions()`; delete last and red | done |
| Success: banner / green text / `alert()` / nothing | one toast, never `alert()` | 6 of 92 routes (#760) |
| "Opslaan" also called "Bewaar" / "Toevoegen" | always "Opslaan" | gate (terminology) |
| Loose hex codes in templates | tokens only | gate |
| Three style assets (stijlgids / ui-conventies / spec) plus an HTML mock | this document + the live route | this document; route #783 |
